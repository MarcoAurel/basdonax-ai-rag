# Archivo de ejemplo para desarrollo local con Docker Desktop
# Copia este archivo como .env.local para desarrollo

# Configuración del modelo LLM
MODEL=phi3

# Modelo de embeddings
EMBEDDINGS_MODEL_NAME=all-MiniLM-L6-v2

# Número de chunks
TARGET_SOURCE_CHUNKS=5

# Para Docker Desktop (Windows/Mac), usar host.docker.internal
# En Linux, usar "chroma" directamente
CHROMA_HOST=host.docker.internal
CHROMA_PORT=8000

# Configuración de Streamlit
STREAMLIT_SERVER_PORT=8080
STREAMLIT_SERVER_ADDRESS=0.0.0.0
